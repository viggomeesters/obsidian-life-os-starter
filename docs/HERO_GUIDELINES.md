# Hero Guidelines

The README hero should stay readable at GitHub README width.

- Use SVG text, not rasterized fuzzy labels.
- Keep labels short and large.
- Avoid decorative lines crossing text.
- Do not include private vault content in visuals.
- Update the version/counts when the schema changes.
