# Roadmap

## Now

- Keep the public repository aligned with the latest vault schema contract.
- Keep `make check` deterministic and dependency-light.
- Preserve the public/private boundary.

## Next

- Add generated Markdown tables from YAML for easier schema review.
- Add example notes for each type using synthetic data only.
- Add JSON Schema export if downstream tooling needs it.

## Later

- Add a small compatibility report between schema versions.
- Add a public-safe migration guide for older vault layouts.
